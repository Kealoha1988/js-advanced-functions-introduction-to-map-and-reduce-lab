// Your code here

// - `mapToNegativize(sourceArray)`
// - `mapToNoChange(sourceArray)`
// - `mapToDouble(sourceArray)`
// - `mapToSquare(sourceArray)`

let mapToNegativize = (array) => {
  a = []
  a.push(array.forEach(element => {element = ~element + 1
  }))
  return a
}

let mapToNochange = (array) => {
 
  return array
}